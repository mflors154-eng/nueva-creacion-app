name: nueva_creacion
description: Ministerio Nueva Creación
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  url_launcher: ^6.3.0
  share_plus: ^10.0.0

flutter:
  uses-material-design: true
